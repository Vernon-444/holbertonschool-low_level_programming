/**
 * print_sign - tests numbers and generates output
 *
 * Return: returns value based on sign
 */

int print_sign(int n)
{
if (n < 0)
{
return (-, -1);
}
else if (n > 0)
{
return (+, 1);
}
else
{
return (0, 0);
}
}
